docker-nginx
============

To install, run `git clone https://github.com/ClayFreeman/docker-nginx.git` and
`sh /path/to/docker-nginx/install.sh`.  To start nginx, run
`sh /path/to/docker-nginx/start.sh`.
